<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.TaskSmasher.repository.ToDoMapper">

	<select id="selectAll" resultType="com.example.TaskSmasher.entity.ToDo">
		SELECT id, todo, detail, created_at as createdAt, updated_at as updatedAt FROM todos
	</select>
	<select id="selectById" resultType="com.example.TaskSmasher.entity.ToDo">
		SELECT id, todo, detail, created_at as createdAt, updated_at as updatedAt FROM todos
		WHERE id = #{id}
	</select>
	
	<select id="selectIncomplete" resultType="com.example.TaskSmasher.entity.ToDo">
		SELECT id, todo, detail, created_at as createdAt, updated_at as updatedAt FROM todos WHERE is_completed = false
	</select>
	<select id="selectComplete" resultType="com.example.TaskSmasher.entity.ToDo">
		SELECT id, todo, detail, created_at as createdAt, updated_at as updatedAt FROM todos WHERE is_completed = true
	</select>
	
<!--	ParameterType="com.example.TaskSmasher.entity.ToDo"は省略可-->
	<insert id="insert">
		INSERT INTO todos (todo, detail, created_at, updated_at) VALUES (#{todo}, #{detail},
		CURRENT_TIMESTAMP, CURRENT_TIMESTAMP )
	</insert>
	<update id="update">
		UPDATE todos SET todo = #{todo}, detail = #{detail}, updated_at = CURRENT_TIMESTAMP
		WHERE id = #{id}
	</update>
	<delete id="delete">
		DELETE FROM todos WHERE id = #{id}
	</delete>
	<update id="complete">
		UPDATE todos SET is_completed = true 
		WHERE id = #{id}
	</update>
	<update id="incomplete">
		UPDATE todos SET is_completed = false 
		WHERE id = #{id}
	</update>
</mapper>